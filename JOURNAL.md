# Project Journal: Adaptive Sequenograms

Этот документ служит дневником разработки и дорожной картой (roadmap) для проекта по извлечению секвенограмм с адаптивной деконволюцией эха.

## 🎯 Глобальная цель
Создать универсальный, быстрый и физически обоснованный алгоритм (на базе PyTorch), который берет "грязную" (реверберирующую) спектрограмму, набор эталонов и выдает:
1. Точное время срабатывания эталонов (секвенограммы).
2. Профиль затухания звука для данной конкретной записи (адаптивное эхо).

---

## ✅ Что уже сделано (Completed)

- [x] **Математическое обоснование:** Разработана гипотеза использования 1D частотно-зависимой свертки вместо 2D для сохранения физического смысла эталонов.
- [x] **Архитектура алгоритма:** 
  - Написаны FFT-свертки (`fft_conv1d`) для обработки массивов неограниченной длины без падения производительности $O(N \log N)$.
  - Реализована сборка чистой спектрограммы без паддинга (zero-padding avoidance) через частотные срезы (`f_start:f_end`).
- [x] **Функция потерь:** Реализована дивергенция Кульбака-Лейблера (KL-Div) + $L_1$-регуляризация (Sparsity) для формирования коротких спайков в секвенограммах.
- [x] **Проверка на синтетике (V&V):** Скрипт `run_synthetic.py` успешно доказал, что алгоритм может вслепую разделить сливающиеся сигналы и восстановить экспоненциальное затухание для разных частот на фоне шума.
- [x] **Инфраструктура проекта:** 
  - Настроена изолированная среда через менеджер пакетов `Pixi`.
  - Настроен автоматический билд научной статьи через компилятор `Tectonic`.
- [x] **Черновик статьи (Draft):** Написан скелет научной статьи в LaTeX (`Article/main.tex`).

---

## 🚧 В процессе (In Progress)

- [ ] **Подготовка реальных данных:** Загрузка массива `(600000, 300)` в память (возможно, потребуется батчинг по времени или перевод в формат `torch.tensor` напрямую из `.npy`).
- [ ] **Извлечение эталонов:** Выделение "чистых" шаблонов `W_k` (размер ~ `100x300`) из реальных спектрограмм летучих мышей.

---

## 📝 Что предстоит сделать (To Do / Plans)

### Фаза 1: Эксперименты на реальных данных
- [ ] Написать скрипт `scripts/run_real_data.py` для прогона алгоритма на спектрограмме летучих мышей.
- [ ] Подобрать гиперпараметры для реальных данных:
  - `learning_rate` (шаг обучения).
  - `lambda_l1` (сила "сжатия" секвенограмм).
  - Инициализация начального окна эха (длина хвоста).
- [ ] Визуализировать результат (сравнить сырую спектрограмму и восстановленную `V_clean`).

### Фаза 2: Написание статьи
- [ ] Сгенерировать красивые PDF-графики результатов (matplotlib).
- [ ] Вставить графики синтетического теста и реального теста в `Article/main.tex`.
- [ ] Дописать раздел "Application to Real-World Acoustic Data".
- [ ] Опубликовать код на GitHub, сделать релиз и получить DOI через Zenodo.

### Фаза 3: Развитие инструмента (Опционально)
- [ ] Упаковка кода в Python-библиотеку (чтобы можно было делать `import sequenograms`).
- [ ] Поддержка CUDA (запуск вычислений на видеокарте). Если тензор `600000x300` не влезет в GPU-память, реализовать метод Overlap-Add для свертки длинных сигналов блоками.

---
*Последнее обновление: $(date)*